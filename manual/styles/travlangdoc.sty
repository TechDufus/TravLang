
%% Support macros for LaTeX documentation generated by travlangdoc.
%% This file is in the public domain; do what you want with it.

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{travlangdoc}
              [2001/12/04 v1.0 travlangdoc support]

\newenvironment{travlangdoccode}{%
  \bgroup
  \leftskip\@totalleftmargin
  \rightskip\z@skip
  \parindent\z@
  \parfillskip\@flushglue
  \parskip\z@skip 
  %\noindent
  \@@par\smallskip
  \@tempswafalse
  \def\par{%
    \if@tempswa
      \leavevmode\null\@@par\penalty\interlinepenalty
  \else
    \@tempswatrue
    \ifhmode\@@par\penalty\interlinepenalty\fi
  \fi}
  \obeylines
  \verbatim@font
  \let\org@prime~%
  \@noligs
  \let\org@dospecials\dospecials
  \g@remfrom@specials{\\}
  \g@remfrom@specials{\{}
  \g@remfrom@specials{\}}
  \let\do\@makeother
  \dospecials
  \let\dospecials\org@dospecials
  \frenchspacing\@vobeyspaces
  \everypar \expandafter{\the\everypar \unpenalty}}
{\egroup\par}

\def\g@remfrom@specials#1{%
  \def\@new@specials{}
  \def\@remove##1{%
    \ifx##1#1\else
    \g@addto@macro\@new@specials{\do ##1}\fi}
  \let\do\@remove\dospecials
  \let\dospecials\@new@specials
  }

\newenvironment{travlangdocdescription}
{\list{}{\rightmargin0pt \topsep0pt}\raggedright\item\noindent\relax\ignorespaces}
{\endlist\medskip}

\newenvironment{travlangdoccomment}
{\list{}{\leftmargin 2\leftmargini \rightmargin0pt \topsep0pt}\raggedright\item\noindent\relax}
{\endlist}

\let \travlangdocparagraph \paragraph
\def \paragraph #1{\travlangdocparagraph {#1}\noindent}
\let \travlangdocsubparagraph \subparagraph
\def \subparagraph #1{\travlangdocsubparagraph {#1}\noindent}

\let\travlangdocvspace\vspace

\newenvironment{travlangdocindent}{\list{}{}\item\relax}{\endlist}
\newenvironment{travlangdocsigend}
     {\noindent\quad\texttt{sig}\travlangdocindent}
     {\endtravlangdocindent\vskip -\lastskip
      \noindent\quad\texttt{end}\medskip}
\newenvironment{travlangdocobjectend}
     {\noindent\quad\texttt{object}\travlangdocindent}
     {\endtravlangdocindent\vskip -\lastskip
      \noindent\quad\texttt{end}\medskip}

\endinput
